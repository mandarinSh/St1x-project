export class Dialog {
    userName: string;
    id: number;

    // public static uniqueIdentifier = 0;

    constructor(userName: string, id: number) {
        this.userName = userName;
        this.id = id;
    }
}
